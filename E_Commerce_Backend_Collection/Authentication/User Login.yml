info:
  name: User Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseUrl}}/login"
  body:
    type: json
    data: "{\r

      \    \"email\": \"tolu@email.com\",\r

      \     \"password\":\"tolu1st\"\r

      }"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "const json = res.getBody();\r

        if(json.token){\r

        \    bru.setEnvVar(\"User_Token\", json.token);\r

        }\r

        \r

        test(\"Login was successful\", function(){\r

        \    expect(res.getStatus()).to.equal(200);\r

        });\r

        \r

        test(\"Token exists\", function (){\r

        \    expect(json.token).to.exist;\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
