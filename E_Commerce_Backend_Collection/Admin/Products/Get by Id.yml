info:
  name: Get by Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseUrl}}/products/{{product_to_delete}}"
  auth:
    type: bearer
    token: "{{Admin_Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
