info:
  name: delete product
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BaseUrl}}/products/{{product_to_delete}}"
  auth:
    type: bearer
    token: "{{Admin_Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
