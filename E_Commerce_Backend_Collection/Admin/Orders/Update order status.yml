info:
  name: Update order status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BaseUrl}}/orders/7/status"
  body:
    type: json
    data: "{\r\n    \"status\":\"pending\"\r\n}"
  auth:
    type: bearer
    token: "{{Admin_Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
